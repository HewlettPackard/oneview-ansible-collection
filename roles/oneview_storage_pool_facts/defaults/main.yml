---
# defaults file for oneview_storage_pool_facts
oneview_storage_pool_facts_config: "{{ role_path }}/files/oneview_config.json"
oneview_storage_pool_facts_contents: "{{ lookup('file', '../config/config.json') | from_json }}"
oneview_storage_pool_facts_config_file: "{{ lookup('file', oneview_storage_pool_facts_config) }}"
