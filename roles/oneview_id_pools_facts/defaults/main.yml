---
# defaults file for oneview_id_pools
oneview_id_pools_facts_config: "{{ role_path }}/files/oneview_config.json"
oneview_id_pools_facts_contents: "{{ lookup('file', '../config/config.json') | from_json }}"
